export enum SYSTEM_CONST {
    minAge = 18,
    maxAge = 100,
    minGrowth = 120,
    maxGrowth = 230,
    lazyLoadingUserCount = 30,
}

export enum SERVER {
    dev = 'dev',
    test = 'test',
    prod = 'prod',
}

export const AUTH_REPOSITORY = 'AUTH_REPOSITORY';
export const PROFILES_REPOSITORY = 'PROFILES_REPOSITORY';
export const LIKES_REPOSITORY = 'LIKES_REPOSITORY';
